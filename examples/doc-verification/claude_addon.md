## Claude Code execution: doc-verification

Use John's Claude dynamic-workflow guidance for large rule-by-chapter fan-outs when available. Preserve the template's Claude workflow assets and fall back to announced inline waves when the feature is absent.

Apply the template and launch the merged plugin with the printed `claude --plugin-dir` command.
