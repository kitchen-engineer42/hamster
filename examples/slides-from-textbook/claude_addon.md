## Claude Code execution: slides-from-textbook

For large slide extraction or rendering fan-outs, use John's Claude dynamic-workflow guidance when the feature is available; otherwise announce the inline fallback. Preserve any template workflow assets under `.claude/workflows/`.

Apply the template and launch the merged plugin with the printed `claude --plugin-dir` command.
