# Python
__pycache__/
*.pyc
*.pyo

# OS junk
.DS_Store

# Editor + IDE
.vscode/
.idea/
*.swp

# Hamster session state — if anyone happens to use this repo as a session
# working dir (not the intended pattern, but possible), don't commit their
# scratch.
forks/
templates/
notes/
.claude/
